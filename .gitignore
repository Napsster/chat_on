# Virtual environment
venv/
env/
ENV/

# Database
*.db
*.sqlite
*.sqlite3

# Python cache
__pycache__/
*.py[cod]
*$py.class
*.so

# Dependencies
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/

# Environment variables
.env
.env.local
.env.*.local

# Uploads directory (but keep structure)
uploads/*
!uploads/.gitkeep

# WhatsApp user data (PII) and downloaded media — never commit
data/users/*
!data/users/.gitkeep
data/uploads/*
!data/uploads/.gitkeep

# Onboarding candidate log (may contain PII) and service account key
data/onboarding_log.csv
data/service_account.json

# Vector embeddings cache (regenerated from knowledge.md on startup)
*.vec.npz

# knowledge.md is generated (knowledge_base.md + active HR-uploaded documents
# from the knowledge_documents DB table) — knowledge_base.md is the tracked file
knowledge.md
.google_drive_sync_cache.json

# Logs
*.log
logs/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Google credentials (NEVER commit)
google-credentials.json

# Backups
*.backup
*.bak
